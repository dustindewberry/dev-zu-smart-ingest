[pytest]
# Minimal pytest config for the step-4 (api skeleton) worktree.
# Once task-1 / task-3 land, the canonical [tool.pytest.ini_options] block
# in pyproject.toml supersedes this file.
asyncio_mode = auto
testpaths = tests
pythonpath = src
filterwarnings =
    ignore::DeprecationWarning
